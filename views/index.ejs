<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>SHUKRANI Pairing</title>
  <style>
    body {
      font-family: sans-serif;
      background: #f9f9f9;
      text-align: center;
      padding: 2em;
    }
    .card {
      background: white;
      padding: 2em;
      max-width: 400px;
      margin: auto;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    input, button {
      padding: 10px;
      margin: 10px 0;
      width: 100%;
      border: 1px solid #ccc;
      border-radius: 5px;
    }
    button {
      background: #0099ff;
      color: white;
      cursor: pointer;
    }
    code {
      background: #eee;
      padding: 5px 10px;
      display: inline-block;
      margin: 10px 0;
      border-radius: 5px;
      font-size: 1.1em;
    }
  </style>
</head>
<body>
  <div class="card">
    <h2>🤖 Pair Your WhatsApp Bot</h2>

    <form method="GET" action="/">
      <input type="text" name="code" placeholder="Enter Phone Number or Code" required>
      <button type="submit">🔐 Generate QR</button>
    </form>

    <% if (qrImage) { %>
      <img src="<%= qrImage %>" alt="QR Code">
      <p>Pair Code:</p>
      <code id="pairCode"><%= pairCode %></code><br>
      <button onclick="copyCode()">📋 Copy Pair Code</button>
    <% } %>
  </div>

  <script>
    function copyCode() {
      const code = document.getElementById("pairCode").textContent;
      navigator.clipboard.writeText(code).then(() => {
        alert("✅ Code copied: " + code);
      });
    }
  </script>
</body>
</html>
