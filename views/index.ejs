<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>SHUKRANI Pairing</title>
  <style>
    body {
      font-family: sans-serif;
      background: #f2f2f2;
      text-align: center;
      padding: 2em;
    }
    .card {
      background: white;
      padding: 2em;
      max-width: 420px;
      margin: auto;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    input, button {
      padding: 10px;
      margin: 10px 0;
      width: 100%;
      border: 1px solid #ccc;
      border-radius: 5px;
    }
    button {
      background: #0099ff;
      color: white;
      cursor: pointer;
    }
    code {
      background: #eee;
      padding: 5px 10px;
      display: inline-block;
      margin: 10px 0;
      border-radius: 5px;
      font-size: 1.1em;
    }
    .links a {
      display: inline-block;
      margin: 8px;
      color: #0077cc;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="card">
    <h2>🤖 SHUKRANI Pairing Panel</h2>

    <form method="GET" action="/">
      <input type="text" name="code" placeholder="Enter Phone Number or Pair Code" required>
      <button type="submit">🔐 Generate QR</button>
    </form>

    <% if (qrImage) { %>
      <img src="<%= qrImage %>" alt="QR Code">
      <p>Pair Code:</p>
      <code id="pairCode"><%= pairCode %></code><br>
      <button onclick="copyCode()">📋 Copy Pair Code</button>
    <% } %>

    <div class="links">
      <hr>
      <a href="https://github.com/SHUKRANI-TECH/shukrani-MD" target="_blank">📦 Bot Repo</a>
      <a href="https://wa.me/255773350309" target="_blank">📱 Contact Me</a>
      <a href="https://whatsapp.com/channel/0029VaDdGH0KVuKmZTqDsM0a" target="_blank">📢 WA Channel</a>
      <a href="https://chat.whatsapp.com/GROUP-LINK" target="_blank">👥 WA Group</a>
    </div>
  </div>

  <script>
    function copyCode() {
      const code = document.getElementById("pairCode").textContent;
      navigator.clipboard.writeText(code).then(() => {
        alert("✅ Code copied: " + code);
      });
    }
  </script>
</body>
</html>
